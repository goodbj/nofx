# 使用Node.js作为基础镜像
FROM node:20-alpine

# 设置工作目录
WORKDIR /app

# 复制package.json和package-lock.json（如果存在）
COPY web/package*.json ./

# 安装依赖
RUN npm ci

# 复制所有web文件到工作目录
COPY web/ ./

# 暴露端口
EXPOSE 3300

# 启动开发服务器
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "3300"]