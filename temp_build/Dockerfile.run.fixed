# 使用轻量级基础镜像运行应用
FROM alpine:latest

# 安装curl用于健康检查（可选）
RUN apk add --no-cache curl ca-certificates

# 设置工作目录
WORKDIR /root/

# 复制预编译的代理服务
COPY binance-proxy .

# 添加执行权限
RUN chmod +x ./binance-proxy

# 暴露端口
EXPOSE 8082

# 运行应用
CMD ["./binance-proxy"]