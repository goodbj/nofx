version: '3.8'

services:
  binance-proxy:
    build:
      context: ./tools_docker_proxy
      dockerfile: Dockerfile
    container_name: binance-proxy-service
    ports:
      - "8081:8081"
    environment:
      - PORT=8081
      - GIN_MODE=release
    restart: unless-stopped