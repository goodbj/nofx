version: '3.8'

services:
  frontend:
    # 使用现有的前端镜像
    image: setup-nofx-dev-frontend
    container_name: nofx-dev-frontend
    ports:
      - "3300:5173"
    volumes:
      - ./web:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - VITE_API_TARGET=http://nofx-dev-backend-fixed:8888  # 指向新的后端服务名
    networks:
      - nofx-dev-net
    restart: unless-stopped

  backend:
    build:
      context: .
      dockerfile: docker/Dockerfile.backend.dev.chrome.fixed
    container_name: nofx-dev-backend-fixed
    ports:
      - "8888:8888"
    volumes:
      - ./api:/app/api
      - ./auth:/app/auth
      - ./backtest:/app/backtest
      - ./config:/app/config
      - ./crypto:/app/crypto
      - ./debate:/app/debate
      - ./decision:/app/decision
      - ./hook:/app/hook
      - ./kernel:/app/kernel
      - ./launcher:/app/launcher
      - ./llm:/app/llm
      - ./logger:/app/logger
      - ./manager:/app/manager
      - ./market:/app/market
      - ./mcp:/app/mcp
      - ./provider:/app/provider
      - ./scripts:/app/scripts
      - ./security:/app/security
      - ./setup:/app/setup
      - ./store:/app/store
      - ./trader:/app/trader
      - ./web:/app/web
      - ./go.mod:/app/go.mod
      - ./go.sum:/app/go.sum
      - ./main.go:/app/main.go
    environment:
      - CHROME_HEADLESS=true
      - CHROME_BIN=/usr/bin/chromium-browser
      - PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
    networks:
      - nofx-dev-net
    working_dir: /app

networks:
  nofx-dev-net:
    driver: bridge