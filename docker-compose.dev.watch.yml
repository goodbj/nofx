services:
  # Backend service (API and core logic) - Development with Hot Reload
  nofx-dev-watch:
    build:
      context: .
      dockerfile: ./docker/Dockerfile.backend.dev.watch
    container_name: nofx-trading-dev-watch
    restart: unless-stopped
    stop_grace_period: 30s
    ports:
      - "8888:8888"  # 使用您指定的后端端口
      - "6060:6060"  # pprof profiling
    volumes:
      - .:/app  # 挂载源代码目录，实现热重载
      - ./data:/app/data
      - /etc/localtime:/etc/localtime:ro
    working_dir: /app  # 设置工作目录
    command: >
      sh -c "
        go mod download &&
        go run main.go
      "
    env_file:
      - .env
    environment:
      - TZ=${TZ:-Asia/Shanghai}
      - AI_MAX_TOKENS=8000
      - API_SERVER_PORT=8888
    networks:
      - nofx-network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8888/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  # Frontend service (static serving and proxy) - Development with Hot Reload
  nofx-frontend-dev-watch:
    build:
      context: .
      dockerfile: ./docker/Dockerfile.frontend
    container_name: nofx-frontend-dev-watch
    restart: unless-stopped
    ports:
      - "3300:80"  # 映射到您指定的前端端口
    volumes:
      - ./web:/build  # 挂载前端源代码目录
    networks:
      - nofx-network
    depends_on:
      - nofx-dev-watch
    environment:
      - NODE_ENV=development
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s

networks:
  nofx-network:
    driver: bridge