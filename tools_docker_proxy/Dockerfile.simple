FROM alpine:latest

# 安装 ca-certificates
RUN apk --no-cache add ca-certificates

WORKDIR /root/

# 复制预编译的二进制文件
COPY nofx-proxy.exe ./nofx-proxy

# 确保二进制文件可执行
RUN chmod +x ./nofx-proxy

# 暴露端口
EXPOSE 8081

# 运行二进制文件
CMD ["./nofx-proxy"]