services:
  nofx-proxy:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tools_docker_proxy
    ports:
      - "${PROXY_PORT:-8081}:8081"
    environment:
      - PORT=8081
      - GIN_MODE=release
    restart: unless-stopped
    networks:
      - nofx-unified-network

networks:
  nofx-unified-network:
    external: true